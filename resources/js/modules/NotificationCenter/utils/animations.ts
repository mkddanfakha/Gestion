import { notificationConfig } from '../config/notification.config'

export const animationClasses = {
    drawerBackdrop: notificationConfig.animations.drawerBackdrop,
    drawerPanel: notificationConfig.animations.drawerPanel,
    toast: notificationConfig.animations.toast,
    listItem: notificationConfig.animations.listItem,
}
